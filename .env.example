# Optional. Prefer setting production URL in src/config.ts.
# If set, it is not read automatically — keep identity in siteConfig.
# PUBLIC_SITE_URL=https://your-domain.com

# Analytics and newsletter are configured in src/config.ts.
# Leave those fields empty until you have a real provider.
